<!DOCTYPE html>
<html>
<head>
  <title>Wt2gt</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav>  

		<%= link_to 'Home*', items_path if logged_in? %>
		<%= link_to_profile current_user.username, 'Me' if logged_in? %>
      <%= link_to 'Friends', users_path if logged_in? %>
      <%= link_to 'Settings', settings_path if logged_in? %>
            <%= link_to 'Log Out', '/sessions/destroy', {:method=>:delete} if logged_in? %>
      <%= link_to 'Log In', '/sessions/new' + request.url unless logged_in? %>
</nav>
  
<%= yield %>

</body>
</html>
